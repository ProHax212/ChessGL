#version 150

in vec2 position;
in vec3 color;
in vec2 texcoords;

out vec3 Color;
out vec2 Texcoords;

void main(){
    Color = color;
    Texcoords = texcoords;
    gl_Position = vec4(position, 0.0, 1.0);
}